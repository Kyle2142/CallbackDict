from setuptools import setup

setup(
    name="CallbackDict",  # Required
    version="1",  # Required
    description="A simple dict extension that uses a (possibly async) callback to fetch missing items",  # Required
    author="https://t.me/Kyle_S",  # Optional
    py_modules=["CallbackDict"]
)
